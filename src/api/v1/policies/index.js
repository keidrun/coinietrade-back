const addPolicy = require('./addPolicy');
const getPolicies = require('./getPolicies');
const getPolicy = require('./getPolicy');
const removePolicy = require('./removePolicy');
const updatePolicy = require('./updatePolicy');

module.exports = {
  addPolicy,
  getPolicies,
  getPolicy,
  removePolicy,
  updatePolicy,
};
